{
  "name": "wheelgpt",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.4.0",
    "@biomejs/biome": "1.9.4"
  },
  "scripts": {
    "lint": "npx @biomejs/biome check --write",
    "test": "npm test --workspace=@wheelgpt/backend"
  }
}