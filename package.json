{
  "name": "wheelgpt",
  "private": true,
  "packageManager": "npm@11.11.1",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.4.6",
    "@types/jest": "^30.0.0",
    "concurrently": "^9.2.1",
    "jest": "^30.3.0",
    "prisma": "^7.5.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "turbo": "^2.8.16",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "glob": "13.0.6"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "dist",
      "node_modules"
    ]
  },
  "scripts": {
    "setup": "npm install && npm run database && npm run build",
    "lint": "npx @biomejs/biome check --write",
    "test": "jest",
    "build": "turbo run build",
    "dev:all": "concurrently \"npm run dev --workspace @wheelgpt/backend\" \"npm start --workspace @wheelgpt/frontend\"",
    "database": "npx prisma migrate deploy && npx prisma generate --sql"
  }
}
